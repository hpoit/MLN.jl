# Assigns a predicate to a conditional random field operator if a predicate has a key constraint (same syntax as LR) and appears in 
# non-recursive rules or rules with linear chain recursion of the form 

# wgt: weight2(l1,l2,wgt), label2(sid,wid1,l1),[wid2=wid1+1]=>label2(sid,wid2,l2)
