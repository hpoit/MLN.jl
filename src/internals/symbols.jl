# Uses a symbol table to convert all logic constants into integer IDs. Since MLN constants are usually typed, keeps track of 
# constant type information. Note: a constant may be shared by multiple types.
